{
  "name": "fem",
  "version": "3.0.0",
  "description": "CSS compiler for the FEM system",
  "scripts": {
    "build": "sass src/scss/fem.scss css/fem.css --style=expanded --no-source-map",
    "watch": "sass src/scss/fem.scss css/fem.css --style=expanded --source-map --watch"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Asacolips",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "sass": "^1.53.0"
  }
}